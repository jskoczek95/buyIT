spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:~/testdb;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driverClassName: org.h2.Driver
